---
// src/components/Process.astro
const steps = [
    {
        day: "H-3 s.d H-0",
        title: "Persiapan Pondasi",
        icon: "fa-solid fa-tractor",
        desc: "Menyiapkan lahan dan fondasi sesuai spesifikasi modular.",
    },
    {
        day: "Hari ke-1",
        title: "Perakitan Rangka",
        icon: "fa-solid fa-helmet-safety",
        desc: "Rangka baja modular mulai berdiri kokoh.",
    },
    {
        day: "Hari ke-2",
        title: "Dinding & Lantai",
        icon: "fa-solid fa-trowel-bricks",
        desc: "Pemasangan dinding, lantai, hingga partisi ruangan.",
    },
    {
        day: "Hari ke-3 & 4",
        title: "Instalasi",
        icon: "fa-solid fa-wrench",
        desc: "Pemasangan pintu, jendela, instalasi air dan kelistrikan.",
    },
    {
        day: "Hari ke-5",
        title: "Finishing",
        icon: "fa-solid fa-sparkles",
        desc: "Interior selesai, unit sudah berdiri sempurna dan siap huni.",
    },
];
---

<section class="process-section" id="proses">
    <div class="container">
        <div class="section-header">
            <span class="section-tag">INSTANT ASSEMBLY</span>
            <h2 class="section-title">
                Selesai dalam <span class="highlight">Hitungan Hari</span>
            </h2>
            <div class="section-divider"></div>
            <p class="section-desc">
                Proyek pembangunan berlarut-larut bisa dihindari. Teknologi <strong
                    >Speed Assembly</strong
                > G-Songuo memungkinkan perakitan sangat cepat.
            </p>
        </div>

        <div class="stats-highlight">
            <div class="stat-item">
                <span class="stat-number">1</span>
                <span class="stat-label"
                    >Unit per Hari <small>(min. 100 unit)</small></span
                >
            </div>
            <div class="stat-item">
                <span class="stat-number">8</span>
                <span class="stat-label">Pekerja Terampil</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">8</span>
                <span class="stat-label"
                    >Hari Total <small>(termasuk 3 hari pondasi)</small></span
                >
            </div>
        </div>

        <div class="timeline">
            {
                steps.map((step, index) => (
                    <div class="timeline-step">
                        <div class="step-icon">
                            <i class={step.icon} />
                        </div>
                        <div class="step-content">
                            <span class="step-day">{step.day}</span>
                            <h4 class="step-title">{step.title}</h4>
                            <p class="step-desc">{step.desc}</p>
                        </div>
                        {index < steps.length - 1 && (
                            <div class="step-connector" />
                        )}
                    </div>
                ))
            }
        </div>

        <p class="process-note">
            <i class="fa-solid fa-circle-info" style="color: var(--c-accent)"
            ></i> *Rumah contoh dibangun dengan total waktu 8 hari. Hari 1 s.d 3 untuk
            penyiapan pondasi. Dibangun oleh tim berjumlah 3 teknisi dan 3 helper.
        </p>
    </div>
</section>

<style>
    .process-section {
        background-color: var(--c-bg-light);
    }
    .container {
        max-width: 1100px;
        margin: 0 auto;
    }
    .section-header {
        text-align: center;
        margin-bottom: 2.5rem;
    }
    .section-tag {
        display: inline-block;
        background: rgba(201, 154, 59, 0.15);
        color: var(--c-accent);
        padding: 0.4rem 1.2rem;
        border-radius: 9999px;
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        margin-bottom: 0.75rem;
    }
    .section-title {
        font-size: clamp(2rem, 4vw, 2.8rem);
        font-weight: 800;
        color: var(--c-text-dark);
        margin-bottom: 0;
    }
    .section-title .highlight {
        color: var(--c-accent);
    }
    .section-desc {
        color: var(--c-text-muted);
        max-width: 650px;
        margin: 0 auto;
        font-size: 1.1rem;
        line-height: 1.6;
    }

    /* Stats */
    .stats-highlight {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 2rem 4rem;
        background: #ffffff;
        border-radius: 1.5rem;
        padding: 2.5rem 1.5rem;
        margin-bottom: 4rem;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        border: 1px solid #e9edf2;
    }
    .stat-item {
        text-align: center;
    }
    .stat-number {
        display: block;
        font-size: 3.5rem;
        font-weight: 900;
        color: var(--c-primary);
        line-height: 1;
        font-family: var(--font-heading);
    }
    .stat-label {
        font-size: 1rem;
        color: var(--c-text-dark);
        font-weight: 600;
        margin-top: 0.5rem;
        display: block;
    }
    .stat-label small {
        display: block;
        font-weight: 400;
        font-size: 0.8rem;
        color: var(--c-text-muted);
        margin-top: 0.2rem;
    }

    /* Timeline */
    .timeline {
        display: flex;
        flex-direction: column;
        gap: 0;
        position: relative;
        margin: 2rem 0;
    }

    @media (min-width: 768px) {
        .timeline {
            flex-direction: row;
            justify-content: space-between;
            align-items: flex-start;
            padding-top: 2rem;
        }
        .step-connector {
            flex: 1;
            height: 3px;
            background: rgba(201, 154, 59, 0.2);
            margin: 0 0.5rem;
            align-self: flex-start;
            margin-top: 2.5rem;
        }
        .timeline-step {
            flex: 1;
            text-align: center;
        }
        .step-content {
            text-align: center;
        }
    }

    .timeline-step {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 0 0.25rem;
        position: relative;
    }
    .step-icon {
        font-size: 1.8rem;
        background: #ffffff;
        color: var(--c-primary);
        width: 4.5rem;
        height: 4.5rem;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 10px 25px -5px rgba(201, 154, 59, 0.2);
        border: 3px solid var(--c-accent);
        margin-bottom: 1.2rem;
        z-index: 2;
        position: relative;
    }

    .step-day {
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--c-text-white);
        background: var(--c-primary);
        padding: 0.2rem 1rem;
        border-radius: 99px;
        display: inline-block;
        margin-bottom: 0.5rem;
    }
    .step-title {
        font-size: 1.1rem;
        font-weight: 700;
        color: var(--c-text-dark);
        margin: 0.3rem 0;
    }
    .step-desc {
        font-size: 0.9rem;
        color: var(--c-text-muted);
        line-height: 1.5;
        max-width: 200px;
        margin: 0 auto;
    }
    @media (max-width: 767px) {
        .timeline-step {
            flex-direction: row;
            align-items: flex-start;
            gap: 1.2rem;
            padding: 1rem 0;
            border-bottom: 1px dashed #e2e8f0;
            width: 100%;
        }
        .timeline-step:last-child {
            border-bottom: none;
        }
        .step-icon {
            width: 3.5rem;
            height: 3.5rem;
            font-size: 1.5rem;
            flex-shrink: 0;
            margin-bottom: 0;
        }
        .step-content {
            text-align: left;
            flex: 1;
        }
        .step-desc {
            max-width: 100%;
            margin: 0;
        }
        .step-connector {
            display: none;
        }
        .stats-highlight {
            gap: 1rem 2rem;
        }
    }

    .process-note {
        text-align: center;
        font-size: 0.9rem;
        color: var(--c-text-muted);
        margin-top: 3.5rem;
        padding: 1.2rem;
        background: white;
        border-radius: 1rem;
        border: 1px solid #e9edf2;
        max-width: 750px;
        margin-left: auto;
        margin-right: auto;
    }
</style>
